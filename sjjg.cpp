// sjjg.cpp : 此文件包含 "main" 函数。程序执行将在此处开始并结束。
//

#include <iostream>
#include "define.h"
#include "Sequence List.h"

int main()
{
    
    
}
/// 查找Lb在La中未出现的元素，将其插入到La表中

/*
SeqList La;
SeqList Lb;
int Arra[] = { 10, 2, 3, 4, 5 };
int Arrb[] = { 3, 4, 5, 6, 7 };
InitSL(&La);
for (int i = 0; i < sizeof(Arra) / sizeof(Arra[0]); i++)
{
    Insert(&La, i + 1, Arra[i]);
}
PrintList(La);

InitSL(&Lb);
for (int i = 0; i < sizeof(Arrb) / sizeof(Arrb[0]); i++)
{
    Insert(&Lb, i + 1, Arrb[i]);
}
PrintList(Lb);

for (int i = 0; i < Lb.length; i++)
{
    int e = Lb.data[i];
    if (LocateElem(La, e) == -1)
    {
        printf("f ");
        Insert(&La, La.length + 1, e);
    }
}

PrintList(La);
*/

